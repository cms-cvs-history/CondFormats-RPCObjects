process DBTest = {
# empty input service, fire events
  source = EmptySource {}
  untracked PSet maxEvents = {untracked int32 input = 1}

  include "CondCore/DBCommon/data/CondDBSetup.cfi"
  es_source RPCCabling = PoolDBESSource {
    using CondDBSetup
    string timetype = "runnumber"
#    string connect = "sqlite_file:RPCEMap_181007.db"
    string connect = "frontier://FrontierDev/CMS_COND_RPC"
    VPSet toGet = {{ string record = "RPCEMapRcd" string tag = "RPCEMap_v2" }}
  }

  module reader =  RPCReadOutMapAnalyzer {
    untracked bool useNewEMap = true
  }

  path p1 = {reader}      
}

