# Configuration file for EventSetupTest_t

process TEST = {

	path p = { get, prod }
	endpath ep = {print}

	es_source = PoolDBESSource { VPSet toGet = 
                                    {
                                     { string record = "RPCReadOutMappingRcd"
                                       string tag    = "RPC-ciccio"
                                     } 
                                    }
		    		    bool loadAll = true
                                    string connect = "sqlite_file:RPCReadOutMap.db"
			            string timetype = "runnumber" 
				   }

	module print = AsciiOutputModule { }
	
	source = EmptySource {untracked int32 maxEvents = 10 untracked uint32 firstRun = 72999 untracked uint32 numberEventsInRun = 2}

	module prod = RPCReadOutMapPrint{}

	module get = EventSetupRecordDataGetter 
                     { 
                       VPSet toGet = 
                       {
	                 {
                           string record = "RPCReadOutMappingRcd"
	                   vstring data = {"RPCReadOutMapping"} 
                         }  
	               }
	               untracked bool verbose = true 
	              }

}
